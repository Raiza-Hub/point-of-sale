const authConfig = {
    providers: [
        {
            domain: "https://glowing-gazelle-84.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
};

export default authConfig;

